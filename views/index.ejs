<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RailRide - Relax and Explore by Train</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
        }

        .hero {
            background-image: url('premium_photo-1661670120218-0738e82d7c07.avif');
            background-size: cover;
            background-position: center;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: #fff;
            padding: 0 20px;
            position: relative;
            overflow: hidden;
        }
        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 1;
        }
        .hero-content {
            position: relative;
            z-index: 2;
        }
        .hero h2 {
            font-size: 48px;
            margin-bottom: 20px;
        }
        .hero p {
            font-size: 20px;
            margin-bottom: 30px;
        }
        .cta-button {
            background-color: #0c3953;
            color: #fff;
            padding: 15px 30px;
            font-size: 18px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            transition: background-color 0.3s;
        }
        .cta-button:hover {
            background-color: #324756;
        }
        @media only screen and (max-width: 768px) {
            .hero h2 {
                font-size: 36px;
            }
            .hero p {
                font-size: 16px;
                margin-bottom: 20px; 
            }

            .cta-button {
                font-size: 16px;
                padding: 10px 20px;
            }
        }
        @media only screen and (max-width: 480px) {
            .hero h2 {
                font-size: 24px;
            }

            .hero p {
                font-size: 14px;
                margin-bottom: 10px;
            }
            .cta-button {
                font-size: 14px;
                padding: 8px 16px;
            }
        }
    </style>
</head>
<body>
    <main>
        <section class="hero">
            <div class="hero-overlay"></div>
            <div class="hero-content">
                <h2>Welcome to RailRide</h2>
                <p>Relax and explore your next adventure by train with RailRide. Start your journey now!</p>
                <a href="/searchtrains" class="cta-button">Search Trains</a>
            </div>
        </section>
    </main>
</body>
</html>
